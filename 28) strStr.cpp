class Solution {
public:
    int strStr(string haystack, string needle) {
        // unordered_map<string,int> mp;
        int x=needle.size();
        return haystack.find(needle);
        return -1;
    }
};
